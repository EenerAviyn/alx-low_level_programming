#include "main.h"

/**
 * _is_upper - checks for uppercase characters
 * @c: character to be checked
 * Return: 1 if c is uppercase, 0 if lowercase
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}

